{
  "name": "amendia-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Full-system Playwright e2e (frontend + backend + stubs + DB). Self-contained: onboards the ACH domain deterministically, runs the browser journeys, tears down. See README.md.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:report": "playwright show-report .report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "js-yaml": "^4.3.1"
  }
}
